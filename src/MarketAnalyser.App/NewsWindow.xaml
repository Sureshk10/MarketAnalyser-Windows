<Window x:Class="MarketAnalyser.App.NewsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="News Impact"
        Width="1180"
        Height="760"
        MinWidth="980"
        MinHeight="640"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="Segoe UI"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style x:Key="PopupButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#243140" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#F2F6FA" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Border Margin="14"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <DockPanel LastChildFill="True">
            <Border DockPanel.Dock="Top" Background="#111A24" Padding="16,12" MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="News Impact" Foreground="White" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="Refresh" Click="RefreshButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Close" Click="CloseButton_Click" Style="{StaticResource PopupButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="Favorites" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding FavoriteText}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding FavoriteNews}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#101720" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Headline}" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding ImpactSummary}" Foreground="#D8E1EC" TextWrapping="Wrap" Margin="0,4,0,0" FontSize="12" />
                                        <TextBlock Text="{Binding Link}" Foreground="#6AA8FF" FontSize="11" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <TabControl Grid.Column="1" Background="Transparent" BorderBrush="{StaticResource PanelBorderBrush}">
                    <TabItem Header="Market">
                        <ListBox ItemsSource="{Binding MarketNews}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#101720" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Headline}" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding ImpactSummary}" Foreground="#D8E1EC" TextWrapping="Wrap" Margin="0,4,0,0" FontSize="12" />
                                            <TextBlock Text="{Binding Link}" Foreground="#6AA8FF" FontSize="11" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                    <TabItem Header="Global">
                        <ListBox ItemsSource="{Binding GlobalNews}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#101720" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Headline}" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding ImpactSummary}" Foreground="#D8E1EC" TextWrapping="Wrap" Margin="0,4,0,0" FontSize="12" />
                                            <TextBlock Text="{Binding Link}" Foreground="#6AA8FF" FontSize="11" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                </TabControl>
            </Grid>
        </DockPanel>
    </Border>
</Window>
