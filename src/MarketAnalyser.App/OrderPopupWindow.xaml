<Window x:Class="MarketAnalyser.App.OrderPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Order Ticket"
        Width="1120"
        Height="720"
        MinWidth="960"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="Segoe UI"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style x:Key="PopupButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#243140" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#F2F6FA" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#35506A" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1A2733" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PopupTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0F141B" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#D8E1EC" />
            <Setter Property="Padding" Value="8,5" />
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#0F141B" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#D8E1EC" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowBackground" Value="#101822" />
            <Setter Property="AlternatingRowBackground" Value="#0D131B" />
            <Setter Property="HorizontalGridLinesBrush" Value="#223142" />
            <Setter Property="VerticalGridLinesBrush" Value="#223142" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A2532" />
            <Setter Property="Foreground" Value="#AFC1D4" />
            <Setter Property="BorderBrush" Value="#263241" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#223142" />
            <Setter Property="Foreground" Value="#D8E1EC" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
    </Window.Resources>

    <Border Margin="14"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <DockPanel LastChildFill="True">
            <Border DockPanel.Dock="Top" Background="#111A24" Padding="16,12" MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock x:Name="TitleText" Foreground="White" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock x:Name="SubtitleText" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="Open" Click="OpenButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Closed" Click="ClosedButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Holdings" Click="HoldingsButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Positions" Click="PositionsButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Exit All" Click="ExitAllButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Close" Click="CloseButton_Click" Style="{StaticResource PopupButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="380" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="ModeText" Foreground="{StaticResource MutedTextBrush}" FontSize="13" Margin="0,0,0,10" />
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Action" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <ComboBox x:Name="SideBox" SelectedIndex="0">
                                <ComboBoxItem Content="Buy" />
                                <ComboBoxItem Content="Sell" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Product" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <ComboBox x:Name="ProductBox" SelectedIndex="2" Width="110">
                                <ComboBoxItem Content="Intraday" />
                                <ComboBoxItem Content="Delivery" />
                                <ComboBoxItem Content="Margin" />
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Order type" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <ComboBox x:Name="OrderTypeBox" SelectedIndex="1">
                                <ComboBoxItem Content="Market" />
                                <ComboBoxItem Content="Limit" />
                                <ComboBoxItem Content="Stop Market" />
                                <ComboBoxItem Content="Stop Limit" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,8,0">
                            <TextBlock Text="Lots" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <TextBox x:Name="QuantityBox" Style="{StaticResource PopupTextBoxStyle}" Text="1" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Lot size" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <TextBlock x:Name="LotSizeText" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,8,0,0" />
                        </StackPanel>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Price" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <TextBox x:Name="PriceBox" Style="{StaticResource PopupTextBoxStyle}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Trigger" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            <TextBox x:Name="TriggerBox" Style="{StaticResource PopupTextBoxStyle}" Text="0" />
                        </StackPanel>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Submit" Click="SubmitButton_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                        <TextBlock x:Name="ActionStatusText" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <TextBlock Text="Strike" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                    <TextBlock x:Name="StrikeText" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,2,0,2" />
                    <TextBlock x:Name="LtpText" Background="#2A1D06" Foreground="#FFD24D" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" Padding="6,2" />
                    <TextBlock x:Name="DetailText" TextWrapping="Wrap" Foreground="#D8E1EC" FontFamily="Consolas" FontSize="12" />
                </StackPanel>

                <TabControl Grid.Column="1" Margin="14,0,0,0">
                    <TabItem Header="Orders">
                        <TabControl>
                            <TabItem Header="Open">
                                <DataGrid x:Name="OrdersGrid">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="120" />
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                                        <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="70" />
                                        <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="*" />
                                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="70" />
                                        <DataGridTextColumn Header="Avg" Binding="{Binding AveragePrice}" Width="80" />
                                        <DataGridTextColumn Header="P/L" Binding="{Binding Pnl}" Width="80" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </TabItem>
                            <TabItem Header="Closed">
                                <DataGrid x:Name="ClosedOrdersGrid">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="120" />
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                                        <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="70" />
                                        <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="*" />
                                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="70" />
                                        <DataGridTextColumn Header="Avg" Binding="{Binding AveragePrice}" Width="80" />
                                        <DataGridTextColumn Header="P/L" Binding="{Binding Pnl}" Width="80" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="Positions">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                <Button Content="Exit Selected" Click="ExitSelectedPosition_Click" Style="{StaticResource PopupButtonStyle}" Margin="0,0,8,0" />
                                <Button Content="Exit All" Click="ExitAllButton_Click" Style="{StaticResource PopupButtonStyle}" />
                            </StackPanel>
                            <DataGrid x:Name="PositionsGrid" SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="*" />
                                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="70" />
                                    <DataGridTextColumn Header="Avg" Binding="{Binding AveragePrice}" Width="80" />
                                    <DataGridTextColumn Header="LTP" Binding="{Binding LastPrice}" Width="80" />
                                    <DataGridTextColumn Header="P/L" Binding="{Binding Pnl}" Width="90" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </TabItem>
                    <TabItem Header="Holdings">
                        <DataGrid x:Name="HoldingsGrid">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Symbol" Binding="{Binding TradingSymbol}" Width="*" />
                                <DataGridTextColumn Header="Qty" Binding="{Binding TotalQty}" Width="70" />
                                <DataGridTextColumn Header="Avg Cost" Binding="{Binding AvgCostPrice}" Width="80" />
                                <DataGridTextColumn Header="LTP" Binding="{Binding LastTradedPrice}" Width="80" />
                                <DataGridTextColumn Header="P/L" Binding="{Binding Pnl}" Width="90" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Grid>
        </DockPanel>
    </Border>
</Window>
